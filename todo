

Optimization:

* swap out the traditional image render target for a storage texel buffer

this will make the expensive copies obsolete

especially important for running on M1


RCS calc:


do real diffraction coefficient calculations

Mitzner:

Look at equations 3-117 and forward

eq 2-58 for definition of nu, guessing that it's like n

alpha is half wedge angle

psi =? phi??
eq 3-47 seems to give that psi is the angle from the face while phi is from some sort of global symmetry
line inside of the wedge, but not sure

Findings:

nu = 1/n

phi is most probably the angle from the axis of symmetry,
this leads to the formulas

psi = phi - (pi - 0.5alpha) = phi - pi*(1 - n/2)

phi = psi + pi*(1 - 0.5n)

Find way of excluding concave angles


Formalize thick lines or look into Bresenham, verify against analytical

Fix geometry and edge extension in vertex shader. Cleaner geometry could probably
fix.


